<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Background -->
    <item android:id="@android:id/background">
        <shape android:shape="rectangle">
            <solid android:color="?attr/budgetSurfaceColor"/>
            <stroke
                android:width="2dp"
                android:color="?attr/budgetTextColor"/>
            <corners android:radius="10dp"/>
        </shape>
    </item>

    <!-- Progress -->
    <item android:id="@android:id/progress">
        <clip>
            <shape android:shape="rectangle">
                <solid android:color="?attr/budgetPrimaryColor"/>
                <stroke
                    android:width="2dp"
                    android:color="?attr/budgetTextColor"/>
                <corners android:radius="10dp"/>
            </shape>
        </clip>
    </item>

</layer-list>
